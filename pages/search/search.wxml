<van-search
  value="{{ value }}"
  shape="round"
  placeholder="请输入搜索关键词"
  show-action
  bind:search="onSearch"
  bind:change="onChange"
  />
  <roomItem wx:for="{{classSearch}}" wx:key="index" cid="{{item.cid}}" capacity="{{item.capacity}}" type="{{item.position}}" position="{{item.name}}" ></roomItem>